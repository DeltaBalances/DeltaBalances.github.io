{
  "name": "deltabalances",
  "version": "0.0.1",
  "main": "",
  "description": "deltabalances utility functions",
  "private": true,
  "dependencies": {
    "bignumber.js": "5.0.0",
    "ethers": "^5.0.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-bundle": "browserify ./deltabalances.js --s bundle -x ./config.js > bundle.js",
    "build-config": "browserify -o configBundle.js -r ./config.js"
  },
  "author": "deltabalances",
  "license": ""
}
